## 0.8.0

- 加入了物品数据组件（Data Components）可视化编辑器
    - 蓝图节点新增 `COMPONENTS` 端口类型；点击该输入端口可打开编辑器
    - `get_inventory_item`、`get_equipment`、`on_item_pickup` 与 `on_use_item` 新增 `COMPONENTS` 输出端口。
    - `give_item` 与 `set_equipment` 新增 `COMPONENTS` 输入端口。
    - 编辑器 UI 参考 CAD Editor（MIT License, Copyright (c) 2022 Franck Velasco；基于 IBE Editor by Skye），已在相关源码头保留署名。
- 修复了 Fabric 版本错误的版本标记
    - 此前将支持的Minecraft版本标记为 ~1.21，导致部分工具错误识别为支持全部 1.21 版本，现更正为 1.21.1
- 修复 `player_tick` 事件
    - 此前仅有常量声明、未注册节点也未分发，现通过 Architectury `TickEvent.PLAYER_POST` 正常触发（每游戏刻为在线玩家触发）。
- 修复 `item_pickup` 事件在 Fabric 端未分发的问题
    - 此前仅 NeoForge 支持，并更新了对应中文描述。
- 修复 `item_pickup` 输出物品 ID 恒为 `minecraft:air` 的问题
    - 拾取发生后物品栈被置空，在事件返回时再读取会得到 air。现于拾取前阶段（Fabric 的 `playerTouch` HEAD / NeoForge 的 `ItemEntityPickupEvent.Pre`）即捕获物品 ID，输出正确的注册表 ID。
- 新增 15 个事件节点
    - 服务器/世界：`on_server_start`、`on_server_stop`、`on_level_load`、`on_explosion`
    - 玩家补充：`on_player_craft`、`on_player_smelt`、`on_player_drop_item`、`on_player_change_dimension`、`on_player_advancement`、`on_player_open_menu`、`on_player_close_menu`、`on_player_chat`、`on_command_perform`
    - 实体补充：`on_entity_tame`、`on_interact_entity`
    - 其中 `craft`/`smelt`/`drop` 复用既有 `ITEM_ID`、`COUNT`、`COMPONENTS`、`ENTITY` 输出端口；`chat`/`command`/`change_dimension`/`advancement`/`open_menu`/`close_menu`/`explosion`/`tame`/`interact_entity` 新增对应输出端口（消息、指令、来自/前往维度、成就 ID、菜单类型、坐标、驯养者、目标实体等）。
    - 事件分发层 `CommonEvents` 统一接入 `LifecycleEvent`、`PlayerEvent`、`InteractionEvent`、`EntityEvent`、`ExplosionEvent`、`ChatEvent`、`CommandPerformEvent`，并在 `MGMCEventContext`/`NodeContext` 中新增维度、成就、菜单、聊天、指令、爆炸等透传字段。
- 修复了 Fabric 版本错误的版本标记。
- 修复了事件分发器的时序问题：原 `EventDispatcher` 在引擎初始化时一次性快照事件节点，导致在 mgmc 之后初始化的附属 mod（如 `mgmc-example`）注册的事件入口节点永远无法被分发。现改为基于注册表版本号（`NodeRegistry.getVersion()`）懒重建事件索引，附属 mod 在任意时机注册的事件节点都会被正确纳入。
